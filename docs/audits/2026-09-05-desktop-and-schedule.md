# 2026-09-05 桌历交互修复与相邻模块审查

范围：以 `8b4bcda` 的桌历、重复事件和通知实现为基线。9 月 5 日先修复桌历原生交互；9 月 6 日按用户追加授权修复下列已确认问题。系统级 Wallpaper Engine 与手动 Win+D 验收仍单独记录。

## 本轮修复

- **桌历鼠标被图标层截获。** 旧版真实窗口父层为 WorkerW；按按钮的实际物理坐标调用 `WindowFromPoint`，返回 `SysListView32`。CDP 点击却能切换视图，因此上一轮 CDP 验收漏掉了系统输入层。新版保持 Electron 顶层窗口，只调整自身 z-order，放在桌面输入层上方、普通应用下方。
- **贴底锚点方向错误。** `GetWindow(..., GW_HWNDNEXT)` 是下方邻窗，旧文档写反。新版识别实际包含 `SHELLDLL_DefView` 的 Progman/WorkerW 顶层宿主，使用其 `GW_HWNDPREV` 作为插入依据，并避免继承 topmost 属性。
- **未经确认的置顶选项。** 用户再次明确桌历始终贴底；移除设置控件和启用路径，清理 SQLite/遗留 JSON 导入中的旧 `alwaysOnTop` 值，保留其他偏好。
- **桌历预加载路径错误。** 从编译后主进程入口启动时，`app.getAppPath()` 指向 `out/main`，导致桥无法加载、页面只剩部分静态外观。改成相对于编译文件的位置；新增 E2E 在原路径上先复现 `deskCalendar === undefined`，修复后验证桥与界面。
- **反复开关累积 IPC 监听器。** 每次创建窗口注册四个全局监听器，原来关闭后没有解绑。现在只处理所属窗口的消息，关闭即解绑并清理拖动起点；E2E 连续三次开关，监听器数量各从 1 回到 0。

## 2026-09-06 追加修复

| 优先级 | 原问题 | 修复与正式证据 |
|---|---|---|
| P1 | 一次性固定事件完成后刷新回到 running。 | 刷新及投影保留 completed；`scheduleRecurrence.regression.test.ts` 覆盖刷新和可见状态。 |
| P1 | 系列删除吞掉已完成历史，未来删除漏掉后续分段。 | 按全局 occurrence 范围记录软删除；保护已完成实例，恢复不改写原状态。永久删除不可恢复，整组删除满 30 天清理；回归覆盖拆分系列。 |
| P1 | 从晚实例编辑系列丢失此前发生日期。 | 使用最早根起点加选中实例的编辑时间差；未来编辑清理被替换的后续分段，保持全局 occurrence ID 与次数。正式 SQLite 的 save/load/periods 回归验证。 |
| P1 | 两条首次并发通知只留下后一条。 | 移除异步 JSON 读改写和全局数组缓存，同步 SQLite 事务在 toast 等待前提交；并发、关闭重开、回滚测试通过。 |
| P2 | 无效重复结束日期被保存为空日程。 | 创建/编辑命令校验日期及范围；桌历拒绝保存时保留草稿并显示可见错误。Electron E2E 通过正式 IPC 验证数据库未改变。 |
| 范围缺口 | 通知记录仍保存 JSON。 | migration 13 新增通知表与一次导入标记；空库权威、坏旧文件保留并报错。备份保持旧格式，任务与通知在同一事务恢复，失败整体回滚。 |

还修复了晚实例编辑沿用首日自定义提醒的问题：主日历、桌历和调度器共用同一时间换算；桌历 datetime-local 输入明确采用上海时区。默认课程/截止提醒在事件尚未开始且错过提前量不足 24 小时时可补收；已过期事件不恢复提醒，消除旧 running 状态导致的误补发。

开发窗口真实操作又复现主日历日视图的普通双击进入拖动保存，导致首日提醒被写入单次覆盖。现在无时间变化或取消的拖动不保存，实际移动按实例保留提醒偏移，调整结束时间保持开始时间不变。已验证双击后数据库不变、第五次系列编辑保留根日期、实际移动半小时后该实例和提醒一起移动。主日历长表单限制在视口内滚动，错误提示位于弹窗内；两窗口最终截图位于 ignored `.tmp/recovery-main-error-final.png` 和 `.tmp/recovery-desk-error-final.png`，仅含隔离 fixture，已亲眼复核。

提醒 24.8 天窗口由既有周期刷新续排，未确认正常运行缺陷，未增加额外计时器。资讯源删除/更新仍只是审查线索，未把它们当作已证实缺陷或本轮交付项。

## 验证边界

- 所有本轮操作使用独立 userData 与 E2E fixture，未修改用户的真实日程和通知。
- 纯域审计与通知并发夹具仅在 Git ignored 的 `.tmp/recurrence-audit/`、`.tmp/notification-audit/`；它们断言的是旧代码的错误行为，测试“通过”不代表功能正确。
- 原生默认贴底状态下已完成鼠标切周视图、双击上游事件、键盘改备注、点击保存，并从正式数据接口确认写入。
- 页面渲染检查与原生输入检查分开记录。脚本 `packages/core/scripts/verify-desktop-input.mjs` 只读取真实窗口命中；被普通应用遮盖返回独立状态，不能误判成桌历已损坏。
- Wallpaper Engine、显示桌面和远端 CI 的最终结果见本轮 `desk-calendar-and-recurrence.md` 自查记录，不沿用上一轮结论。
