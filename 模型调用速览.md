# CampusOS 模型调用速览

本文件是本工作区的长期协作记忆。模型可用性、额度与实际速度以运行时界面为准；下面的选择按任务风险、复杂度和返工成本决定。

## 默认分工

- 主线程默认使用 `GPT-5.6 Sol / medium`，负责需求判断、方案取舍、关键修改和最终验收。
- 独立且范围明确的查找、资料归纳、测试日志分析和交叉核验可交给 `GPT-5.6 Luna / medium` 子代理；复杂链路核验使用 `Luna / high`。
- 只在需要时并发子代理；主线程必须复核关键结论，避免把子代理摘要当作最终事实。

## 按工作阶段选择

| 场景 | 首选 | 升级条件 |
| --- | --- | --- |
| 新对话熟悉项目、建立项目地图 | `Terra / medium`；若马上开发则 `Sol / medium` | 仓库大且模块可独立探索时，用 `Luna / medium` 分头只读检查 |
| 功能需求、范围和交互讨论 | `Sol / medium` | 架构或领域约束多时升 `Sol / high` |
| 外部调研、对照实现和技术选型 | `Sol / medium` + `Luna / medium` 证据搜集 | 涉及认证、会话、缓存、迁移或安全边界时 `Sol / high` |
| 范围明确的常规功能实现 | `Sol / medium` | 跨模块状态、认证、同步、缓存或复杂数据投影时 `Sol / high` |
| 人工验收发现的明确 UI/交互问题 | `Terra / medium` 或 `Sol / medium` | 问题涉及真实数据、IPC、状态恢复或无法稳定复现时 `Sol / high` |
| 连续两轮返工仍失败、根因不明 | `Sol / high`，先重建复现证据 | 独立复核后仍无法收敛时 `Astra / high` |
| 已定位且约束密集的高风险问题 | `Astra / high` | 仅在问题确实需要时使用 `xhigh`；`max`/`ultra` 不是日常默认 |

## 选择原则

1. 先用能可靠完成任务的最低档位：`medium` 是默认，`high` 用于复杂推理或高返工风险。
2. 缺日志、复现步骤、真实上游反馈时，先补证据，不能只靠提高模型或强度。
3. 涉及 CampusOS 的 Celechron 重合业务，先阅读对照实现；涉及 UI，必须按项目视觉验收流程验证真实界面。
4. 长时间任务应分为“调研/诊断 → 方案确认 → 实现 → 验证”阶段；不应因为任务时长直接常驻最高模型和最高强度。

## 可直接给 Codex 的提示

> 这轮按性价比优先：主线程使用 Sol / medium；独立探索交给 Luna / medium；只有认证、同步、复杂状态或反复返工时升为 high。遇到认为需要升级主模型的难点，先说明证据和原因。
