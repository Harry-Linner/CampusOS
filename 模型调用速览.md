# CampusOS 模型调用速览

本文件是本工作区的长期协作记忆。以下为用户确认的任务分工偏好（2026-09-05 更新）。模型可用性、推理档位、额度和速度在运行时核实；实际主模型与强度由用户在 Codex 界面选择，文字提示不代表已经完成切换。

## 默认分工

- 主线程默认选择 **GPT-6 Astra / high**，负责需求边界、方案取舍、关键代码、结果复核与最终验收。
- 讨论、解释代码和边界明确的小改可用 **Astra / medium**；跨模块长任务、复杂根因和真人验收返工优先 **Astra / xhigh**。
- **max 不常驻**：连续两次返工仍不通过、根因难以收敛或范围明确的高风险审查时再考虑；先重新检查假设和证据。
- **GPT-5.6 Luna / medium** 子代理承担独立查找、分目录探索、资料归纳、日志和测试分析；复杂链路或独立审计使用 **Luna / high**。
- 子代理任务应范围明确、互不重叠，默认只读。主线程必须针对原文件、调用链或来源复核重要结论，并保留关键写入与最终验收。
- 并发数量以实际会话支持为准；需要时使用 2～3 个独立子任务，不能为了用满并发而拆分紧耦合问题。

## 按任务选择

| 使用场景 | 主模型与强度 | 子代理与工作方式 |
| --- | --- | --- |
| 简单问答、解释代码、小文档调整 | Astra / medium | 通常不用 |
| 新对话熟悉大型项目、建立项目地图 | Astra / high | Luna / medium 分目录探索；主线程复核关键结论 |
| 产品讨论、功能边界、架构设计 | Astra / high | 必要时 Luna / high 独立核对文档与实现 |
| 外部调研、对照实现和技术选型 | Astra / high；复杂认证或迁移升 xhigh | Luna / medium 搜集来源；复杂源码追踪用 high |
| 边界明确的小功能 | Astra / medium | 必要时 Luna / medium 查入口或分析测试 |
| 跨模块正式开发 | Astra / high | Luna / medium 或 high 探索与复核 |
| SQLite、认证、数据迁移、复杂状态机 | Astra / xhigh | Luna / high 独立审计 |
| Electron 多窗口、WorkerW、难复现问题 | Astra / xhigh | Luna / high 收集证据，主线程建立复现和诊断闭环 |
| 真人验收不通过后的返工 | Astra / xhigh | 先复现实际操作；必要时 Luna / high 独立审查根因 |
| 连续两次返工仍不通过 | Astra / max | 重新审查需求理解、假设、链路和测试，不继续盲目补丁 |
| 普通日志分类、文件盘点、测试结果归纳 | Luna / medium | 可独立承担，关键结论由主线程复核 |

这些档位是本项目的经验性选择，不能保证某个模型或强度一定成功。任务长短本身不决定强度，跨模块约束和返工风险更重要。

## 三个常用场景

1. **首次了解项目：Astra / high。** 读取根目录上下文、核心文档、代码入口和 Git 状态；独立模块交给 Luna / medium，主线程输出并复核项目地图。
2. **开发新功能：Astra / high。** 先明确功能边界和验收条件。涉及复杂跨模块状态、原生窗口、认证或数据迁移时升 xhigh；Luna 负责外部实现、现有代码和测试入口调查。
3. **真人验收失败：Astra / xhigh。** 先用用户的实际操作复现，再修改。连续两轮仍失败再考虑 max，同时重新审查假设；不能仅提高强度后继续原来的补丁思路。

这次“重复规则 + SQLite + 提醒调度 + 双窗口 UI + Windows 桌面层”的组合任务，推荐 **Astra / xhigh 主线程 + Luna / high 独立探索与审计**。边界已经完全确定且风险较低的实现，可用 Astra / high + Luna / medium。

## 质量与成本原则

- 强模型主要用于判断、关键实现和验收；大范围搜索与读重工作优先交给 Luna，以减少昂贵上下文和重复阅读。
- 缺日志、复现、真实上游反馈时先补证据。更高推理强度不能代替真实输入和输出闭环。
- 涉及 Celechron 重合业务先读对照实现；涉及界面必须亲自查看真实渲染。原生桌面输入问题还必须验证 Windows 实际鼠标命中与点击，CDP 直接操作页面不能证明桌面层可交互。
- 每次最终修改后，重跑受影响的检查。测试文件修改也需要类型检查，不能将改动前通过的结果用于新提交。
- 长任务按诊断或调研、边界确定、实现、验证推进；已获授权的工作持续完成，不反复索要同一项授权。
- API 标价仅用于理解相对成本，不等于 Codex 套餐的额度折算。价格、额度和速度不写成长期事实，实际需要时重新查询。

## 可直接使用的提示

> 熟悉项目：读取当前目录上下文、核心文档、代码入口与 Git 状态，不修改文件。独立探索交给 Luna / medium，关键结论由主线程复核。给我简洁的项目地图与明显风险。

> 正式开发：按已确认边界完成实现、文档同步、真实交互验收、提交推送和当前提交的 CI 闭环。独立探索与审计交给 Luna，主线程负责方案、关键修改和验收。

> 验收返工：先按我的实际操作复现失败，再列出可验证的根因假设。必要时让 Luna / high 独立复核；修复后重新走原始失败链路，不以单测或截图代替实际交互。

## 官方依据

核对日期：2026-09-05。官方将 Astra 定位于复杂端到端工作，将 Luna 定位于成本敏感的工作；本文件的具体强度分配为项目协作建议。

- [GPT-6 Astra 模型说明](https://developers.openai.com/api/docs/models/gpt-6-astra)
- [OpenAI 模型目录](https://developers.openai.com/api/docs/models)
