node_modules/
.pnpm-store/
.turbo/
dist/
out/
coverage/
.tmp/
playwright-report/
test-results/
*.log
.env
.env.*
*.env
!.env.example
.agents/
.claude/

# 内部文档与维护者专用脚本（人类活动记录、私有台账、真账号验收/重构工具）
# 这些内容不进公开仓库，但保留在本地工作区供维护者与协作 agent 使用。
docs-internal/
scripts-internal/

# 全部文档均不公开：产品文档、设计决策、发布说明与截图一律留在维护者本地。
/docs/

# 协作 agent 的本地工作区指令：含私有基线约束、内部文档路径与个人环境信息，不入公开仓库。
/AGENTS.md
/CLAUDE.md

# 已停止开发的早报（daily-brief）插件：2026-08-25 移出官方模块名单，不再接线；
# 文件保留在本地以便日后恢复。core 侧保留的早报服务与 IPC 不再依赖它。
/plugins/official/daily-brief/

# 本地诊断与 .dsh 备份（可能含会话/路径等隐私信息，禁止入库）
/diagnostics-*.zip
/.dsh.before.zip

# dsh-vision-toolkit 的临时截图 Artifact（视觉验收产物，不入库）
/.dsh-vision-toolkit/
